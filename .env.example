# Wikipedia MCP Server Configuration

# User agent for Wikipedia API requests (REQUIRED - should include contact information)
WIKIPEDIA_USER_AGENT=Wikipedia-MCP/1.0 (https://github.com/user/wikipedia-mcp; contact@example.com)

# Wikipedia language edition (default: en)
# Supported: en, es, fr, de, it, pt, ru, ja, zh, ar, etc.
WIKIPEDIA_LANGUAGE=en

# Delay between requests in milliseconds (default: 1000)
# Minimum recommended: 1000ms to respect rate limits
WIKIPEDIA_REQUEST_DELAY=1000

# Request timeout in milliseconds (default: 30000)
WIKIPEDIA_TIMEOUT=30000